.include "system.mac"
.macro      debug_string s
            .local mystring
.data
mystring:   
            .asciiz s
.code
            saveall
            lda #15
            sta s_y
            lda #0
            sta s_x
            pushw ptr_0
            mov #mystring, ptr_string
            jsr _draw_string
            popw ptr_0
            resall
.endmacro
